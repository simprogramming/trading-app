<div class="container-hot-stocks">
  <div class="d-flex">
    <div class="card-hot-stocks">
      <div class="top scroll">
        <div class="card-category text-center">Monday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"> <%= Date.today.monday.day + 7 %> </p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 7) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div class="scroll">
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 7) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top scroll">
        <div class="card-category text-center">Tuesday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 8).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 8) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div class="scroll">
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 8) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top scroll">
        <div class="card-category text-center">Wednesday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 9).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 9) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div class="scroll">
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 9) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top scroll">
        <div class="card-category text-center">Thursday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 10).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 10) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div class="scroll">
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 10) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top scroll">
        <div class="card-category text-center">Friday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 11).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where(date: (Date.today.monday + 11) - 4, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div class="scroll">
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 11) - 4, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker" data-toggle="tooltip" data-placement="top" title=<%= hot_stock.user.nickname %>><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>
  </div>
</div>
