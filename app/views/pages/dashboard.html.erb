<div class="goldbar-dashboard"></div>
<div class="container-hot-stocks">
  <div class="d-flex">
    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Monday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"> <%= Date.today.monday.day %> </p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div>
        <p>
          <% @hot_stocks.where( date: (Date.today.monday) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Tuesday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 1).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 1) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div>
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 1) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Wednesday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 2).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 2) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div>
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 2) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Thursday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 3).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where( date: (Date.today.monday + 3) - 6, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div>
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 3) - 6, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Friday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 4).day %></p>
          <p><%= image_tag "up-arrow.svg", class: "avatar-watchlist" %></p>
        </div>
        <div>
          <p>
            <% @hot_stocks.where(date: (Date.today.monday + 4) - 4, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
            <% end %>
          </p>
        </div>
      </div>
      <p class="text-right"><%= image_tag "892624.svg", class: "avatar-watchlist" %></p>
      <div>
        <p>
          <% @hot_stocks.where( date: (Date.today.monday + 4) - 4, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker"><%= hot_stock.stock.symbol%></span>
          <% end %>
        </p>
      </div>
    </div>
  </div>
<div class="goldbar"></div>
  <div class="d-flex">
    <div class="broker-form">
      <%= simple_form_for [@position] do |f| %>
      <%= f.input :stock_id, collection: Stock.pluck(:symbol, :id), label: 'Symbol', input_html: {class: 'select2', id:'select_stock_id'}  %>
      <%= f.input :buy_sell, as: :radio_buttons, collection: ['Buy', 'Sell'], include_blank: false, label: "Entry Action"  %>
      <%= f.input :size %>
      <%= f.input :entry, label: "Price", input_html: {readonly: "readonly"} %>
      <%= f.input :baseline %>
      <%= f.input :target, label: "Target Price" %>
      <%= f.input :stop_loss, label: "Stop Loss" %>

      <%= f.button :submit, class: "btn-secondary rounded-btn" %>
      <% end %>
    </div>
    <!-- TradingView Widget BEGIN -->
    <div class="tradingview-widget-container">
      <div id="tradingview_ffdae"></div>
      <div class="tradingview-widget-copyright"><a href="https://www.tradingview.com/symbols/AMEX-SPY/" rel="noopener" target="_blank"><span class="blue-text">SPY Chart</span></a> by TradingView</div>
      <script type="text/javascript" src="https://s3.tradingview.com/tv.js"></script>
      <script type="text/javascript">
      new TradingView.widget(
      {
      "width": 1190,
      "height": 705,
      "symbol": "AMEX:SPY",
      "interval": "W",
      "timezone": "Etc/UTC",
      "theme": "dark",
      "style": "2",
      "locale": "en",
      "toolbar_bg": "#f1f3f6",
      "enable_publishing": false,
      "hide_side_toolbar": false,
      "allow_symbol_change": true,
      "container_id": "tradingview_ffdae"
      }
      );
      </script>
    </div>
    <!-- TradingView Widget END -->
  </div>
</div>

