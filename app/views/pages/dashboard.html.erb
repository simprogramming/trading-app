
<h1>Dashboard</h1>

<%= link_to "home", root_path %>

<div class="container">
  <h2>Broker</h2>
  <div class="border form row">
    <div class="col-md-8">
      <%= simple_form_for [@position] do |f| %>
      <%= f.input :stock_id, collection: Stock.pluck(:symbol, :id), label: 'Symbol' %>
      <%= f.input :entry_1, as: :check_boxes, collection: ['Buy', 'Sell'], include_blank: false, label: "Entry Action"  %>
      <%= f.input :quantity_1 %>
      <%= f.input :baseline %>
      <%= f.input :target, label: "Target Price" %>
      <%= f.input :stop_loss, label: "Stop Loss" %>

      <%= f.button :submit, class: "btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
