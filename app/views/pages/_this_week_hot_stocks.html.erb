<div class="container-hot-stocks">
  <div class="d-flex">
    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Monday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"> <%= Date.today.monday.day %> </p>
          <div class="text-right"><%= render partial: 'new_hot_stock', locals: { date: Date.today.monday, direction: 'Buy' } %></div>
        </div>
        <div class="scroll-top-card">
          <p>
            <% current_user.hot_stocks.where( date: Date.today.monday, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker_buy"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <div class="d-flex justify-content-between">
        <p></p>
        <div class="down text-right">
          <%= render partial: 'new_hot_stock', locals: { date: Date.today.monday, direction: 'Sell' } %>
        </div>
      </div>
      <div class="scroll-down-card">
        <p>
          <% current_user.hot_stocks.where( date: Date.today.monday, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker_sell"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Tuesday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 1).day %></p>
          <div class="text-right"><%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 1, direction: 'Buy' } %></div>
        </div>
        <div class="scroll-top-card">
          <p>
            <% current_user.hot_stocks.where( date: Date.today.monday + 1, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker_buy"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <div class="d-flex justify-content-between">

        <p></p>

        <div class="down text-right">
          <%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 1, direction: 'Sell' } %>
        </div>
      </div>
      <div class="scroll-down-card">
        <p>
          <% current_user.hot_stocks.where( date: Date.today.monday + 1, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker_sell"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Wednesday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 2).day %></p>
          <div class="text-right"><%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 2, direction: 'Buy' } %></div>
        </div>
        <div class="scroll-top-card">
          <p>
            <% current_user.hot_stocks.where( date: Date.today.monday + 2, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker_buy"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <div class="d-flex justify-content-between">

        <p></p>

        <div class="down text-right">
          <%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 2, direction: 'Sell' } %>
        </div>
      </div>
      <div class="scroll-down-card">
        <p>
          <% current_user.hot_stocks.where( date: Date.today.monday + 2, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker_sell"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Thursday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 3).day %></p>
          <div class="text-right"><%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 3, direction: 'Buy' } %></div>
        </div>
        <div class="scroll-top-card">
          <p>
            <% current_user.hot_stocks.where( date: Date.today.monday + 3, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker_buy"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <div class="d-flex justify-content-between">

        <p></p>

        <div class="down text-right">
          <%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 3, direction: 'Sell' } %>
        </div>
      </div>
      <div class="scroll-down-card">
        <p>
          <% current_user.hot_stocks.where( date: Date.today.monday + 3, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker_sell"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>

    <div class="card-hot-stocks">
      <div class="top">
        <div class="card-category text-center">Friday</div>
        <div class="d-flex justify-content-between">
          <p class="text-left"><%= (Date.today.monday + 4).day %></p>
          <div class="text-right"><%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 4, direction: 'Buy' } %></div>
        </div>
        <div class="scroll-top-card">
          <p>
            <% current_user.hot_stocks.where( date: Date.today.monday + 4, direction: 'Buy').each do |hot_stock| %>
              <span class="stock_ticker_buy"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
            <% end %>
          </p>
        </div>
      </div>
      <div class="d-flex justify-content-between">

        <p></p>

        <div class="down text-right">
          <%= render partial: 'new_hot_stock', locals: { date: Date.today.monday + 4, direction: 'Sell' } %>
        </div>
      </div>
      <div class="scroll-down-card">
        <p>
          <% current_user.hot_stocks.where( date: Date.today.monday + 4, direction: 'Sell').each do |hot_stock| %>
            <span class="stock_ticker_sell"><%= link_to hot_stock.stock.symbol, hot_stock_path(hot_stock), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
      </div>
    </div>
  </div>
  <div class="d-flex">

    <div class="derivatives-card">
      <div class="d-flex justify-content-between">
        <p></p>
        <div class="down text-right">
          <%= render partial: 'new_ema', locals: { date: Date.today.monday, direction: 'Buy' } %>
        </div>
      </div>
        <p class="scroll-down-ema-card">
          <% current_user.emas.where( date: Date.today.monday, direction: 'Buy').each do |ema| %>
            <span class="ema_ticker_buy"><%= link_to ema.stock.symbol, ema_path(ema), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
          <% end %>
        </p>
    </div>
    <div class="derivatives-card">
      <div class="d-flex justify-content-between">
        <p></p>
        <div class="down text-right">
          <%= render partial: 'new_ema', locals: { date: Date.today.monday + 1, direction: 'Buy' } %>
        </div>
      </div>
      <p class="scroll-down-ema-card">
        <% current_user.emas.where( date: Date.today.monday + 1, direction: 'Buy').each do |ema| %>
          <span class="ema_ticker_buy"><%= link_to ema.stock.symbol, ema_path(ema), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
        <% end %>
      </p>
    </div>
    <div class="derivatives-card">
      <div class="d-flex justify-content-between">
        <p></p>
        <div class="down text-right">
          <%= render partial: 'new_ema', locals: { date: Date.today.monday + 2, direction: 'Buy' } %>
        </div>
      </div>
      <p class="scroll-down-ema-card">
        <% current_user.emas.where( date: Date.today.monday + 2, direction: 'Buy').each do |ema| %>
          <span class="ema_ticker_buy"><%= link_to ema.stock.symbol, ema_path(ema), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
        <% end %>
      </p>
    </div>
    <div class="derivatives-card">
      <div class="d-flex justify-content-between">
        <p></p>
        <div class="down text-right">
          <%= render partial: 'new_ema', locals: { date: Date.today.monday + 3, direction: 'Buy' } %>
        </div>
      </div>
      <p class="scroll-down-ema-card">
        <% current_user.emas.where( date: Date.today.monday + 3, direction: 'Buy').each do |ema| %>
          <span class="ema_ticker_buy"><%= link_to ema.stock.symbol, ema_path(ema), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
        <% end %>
      </p>
    </div>
    <div class="derivatives-card">
      <div class="d-flex justify-content-between">
        <p></p>
        <div class="down text-right">
          <%= render partial: 'new_ema', locals: { date: Date.today.monday + 4, direction: 'Buy' } %>
        </div>
      </div>
      <p class="scroll-down-ema-card">
        <% current_user.emas.where( date: Date.today.monday + 4, direction: 'Buy').each do |ema| %>
          <span class="ema_ticker_buy"><%= link_to ema.stock.symbol, ema_path(ema), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></span>
        <% end %>
      </p>
    </div>
  </div>
</div>
