# require 'rails_helper'

# RSpec.describe HotStocksController, type: :controller do
  # let(:user) { create :user }
  # let(:stock) { create :stock }
  # # let(:valid_attributes) { attributes_for :stock }
  #
  #
  # before { sign_in user }
  #
  # describe 'POST #create' do
  #   # hot_stock1 = HotStock.create!(user_id:, stock_id: stock.id, date: Time.now, direction: 'Buy' )
  #   let(:post!) { post :create, params: { hot_stock:  } }
  #
  #   context 'with valid params' do
  #
  #     it { expect { post! }.to change(HotStock, :count).by(1) }
  #
  #     context 'when called' do
  #       before { post! }
  #
  #       it { expect(response).to redirect_to(new_stock_path) }
  #     end
  #   end
  #
  # end
# end